import numpy as np
import matplotlib.pyplot as plt
import supervised_learning_ml_training_server.utils.cnn_utils as cnn_utils
import data.data_utils as load_data


def cnn_model(x, y, dim_of_layers, learning_rate, num_of_iterations):
    pass
